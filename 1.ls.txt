ls是（list directory contents）的缩写。，运行它就可以可以列出文件夹中内容。
其实我觉得tree命令比这个对文件目录更加清晰，只不过需要安装tree。
ls:[命令显示文件中有那些文件（夹）]
liunx下20个常用命令
ls -l:[命令以详情模式（long listing fashion）列出文件中的内容]
总用量 4
drwxrwxr-x 2 lei lei 4096 2月  12 17:56 liunx下20个常用命令
/*d代表是目录。rwxr-x 是文件或者目录对所属用户，同一组用户和其他用户对他的权限。
紧跟着是4096代表字节数（文件好像不大小好像是4k的整数倍）。后面是最后被修改的日期。最后就是包含的文件（夹）了*/
ls -a:[会把.开头的隐藏文件显示出来]
.  ..  liunx下20个常用命令