chmod改变文件的模式位（权限）。
/*三种权限rwx（拥有读写和执行权限）。 r-x（只有读和执行权限，没有写权限）。-x（只有执行权限）*/
sudo chmod -R 777 liunx下20个常用命令:
/*该命令可以去掉文件夹的锁，避免某些文件夹每次都sudo才能写*/
/*-R 表示对文件目录下的所有文件都这样设置（相同权限的变更），777是设置所有人都可读可写可执行（貌似是最高权限），其他数字（600，644，700，755，711，666，这些自行查阅）*/